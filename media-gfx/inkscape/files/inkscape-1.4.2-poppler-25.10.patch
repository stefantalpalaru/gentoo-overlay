diff '--color=auto' -ur inkscape-1.4.2.orig/src/extension/internal/pdfinput/pdf-parser.cpp inkscape-1.4.2/src/extension/internal/pdfinput/pdf-parser.cpp
--- inkscape-1.4.2.orig/src/extension/internal/pdfinput/pdf-parser.cpp	2025-11-08 01:09:26.128340791 +0100
+++ inkscape-1.4.2/src/extension/internal/pdfinput/pdf-parser.cpp	2025-11-08 01:10:31.624040844 +0100
@@ -2258,7 +2258,7 @@
     auto font = state->getFont();
     int wMode = font->getWMode(); // Vertical/Horizontal/Invalid
 
-    builder->beginString(state, s->getLength());
+    builder->beginString(state, get_goostring_length(*s));
 
     // handle a Type 3 char
     if (font->getType() == fontType3) {
@@ -2269,7 +2269,7 @@
     state->textTransformDelta(0, state->getRise(), &riseX, &riseY);
 
     auto p = s->getCString(); // char* or const char*
-    int len = s->getLength();
+    int len = get_goostring_length(*s);
 
     while (len > 0) {
 
diff '--color=auto' -ur inkscape-1.4.2.orig/src/extension/internal/pdfinput/poppler-utils.cpp inkscape-1.4.2/src/extension/internal/pdfinput/poppler-utils.cpp
--- inkscape-1.4.2.orig/src/extension/internal/pdfinput/poppler-utils.cpp	2025-11-08 01:09:26.129048804 +0100
+++ inkscape-1.4.2/src/extension/internal/pdfinput/poppler-utils.cpp	2025-11-08 01:13:05.841334581 +0100
@@ -168,7 +168,7 @@
         case objString:
             h->hash('s');
             s = obj->getString();
-            h->hash(s->c_str(), s->getLength());
+            h->hash(s->c_str(), get_goostring_length(*s));
             break;
         case objName:
             h->hash('n');
@@ -586,10 +586,10 @@
 std::string getString(const GooString *value)
 {
     if (_POPPLER_HAS_UNICODE_BOM(value)) {
-        return g_convert(value->getCString () + 2, value->getLength () - 2,
+        return g_convert(value->getCString () + 2, get_goostring_length(*value) - 2,
                          "UTF-8", "UTF-16BE", NULL, NULL, NULL);
     } else if (_POPPLER_HAS_UNICODE_BOMLE(value)) {
-        return g_convert(value->getCString () + 2, value->getLength () - 2,
+        return g_convert(value->getCString () + 2, get_goostring_length(*value) - 2,
                          "UTF-8", "UTF-16LE", NULL, NULL, NULL);
     }
     return value->toStr();
diff '--color=auto' -ur inkscape-1.4.2.orig/src/extension/internal/pdfinput/poppler-utils.h inkscape-1.4.2/src/extension/internal/pdfinput/poppler-utils.h
--- inkscape-1.4.2.orig/src/extension/internal/pdfinput/poppler-utils.h	2025-11-08 01:09:26.129107184 +0100
+++ inkscape-1.4.2/src/extension/internal/pdfinput/poppler-utils.h	2025-11-08 01:14:03.804069132 +0100
@@ -19,6 +19,7 @@
 #include <string>
 #include <unordered_set>
 #include <vector>
+#include <goo/GooString.h>
 
 #include "poppler-transition-api.h"
 
@@ -104,4 +105,12 @@
     void hashFontObject1(const Object *obj, FNVHash *h);
 };
 
+inline size_t get_goostring_length(const GooString& str) {
+#if POPPLER_CHECK_VERSION(25, 10, 0)
+    return str.size();
+#else
+    return str.getLength();
+#endif
+}
+
 #endif /* POPPLER_UTILS_H */
