diff '--color=auto' -ur blender-4.5.10.orig/intern/cycles/device/CMakeLists.txt blender-4.5.10/intern/cycles/device/CMakeLists.txt
--- blender-4.5.10.orig/intern/cycles/device/CMakeLists.txt	2026-06-21 00:52:40.451081713 +0200
+++ blender-4.5.10/intern/cycles/device/CMakeLists.txt	2026-06-21 00:54:00.841854338 +0200
@@ -10,7 +10,7 @@
 
 if(WITH_CYCLES_DEVICE_OPTIX OR WITH_CYCLES_DEVICE_CUDA)
   if(NOT WITH_CUDA_DYNLOAD)
-    add_definitions(-DCYCLES_CUDA_NVCC_EXECUTABLE="${CUDA_NVCC_EXECUTABLE}")
+    add_definitions(-DCYCLES_CUDA_NVCC_EXECUTABLE="nvcc")
   endif()
 
   add_definitions(-DCYCLES_RUNTIME_OPTIX_ROOT_DIR="${CYCLES_RUNTIME_OPTIX_ROOT_DIR}")
diff '--color=auto' -ur blender-4.5.10.orig/intern/cycles/kernel/CMakeLists.txt blender-4.5.10/intern/cycles/kernel/CMakeLists.txt
--- blender-4.5.10.orig/intern/cycles/kernel/CMakeLists.txt	2026-06-21 00:52:40.779939078 +0200
+++ blender-4.5.10/intern/cycles/kernel/CMakeLists.txt	2026-06-21 00:54:50.106377686 +0200
@@ -463,7 +463,7 @@
   set(CUDA_BITS 64)
 
   # CUDA version
-  execute_process(COMMAND ${CUDA_NVCC_EXECUTABLE} "--version" OUTPUT_VARIABLE NVCC_OUT)
+  execute_process(COMMAND nvcc "--version" OUTPUT_VARIABLE NVCC_OUT)
   string(REGEX REPLACE ".*release ([0-9]+)\\.([0-9]+).*" "\\1" CUDA_VERSION_MAJOR "${NVCC_OUT}")
   string(REGEX REPLACE ".*release ([0-9]+)\\.([0-9]+).*" "\\2" CUDA_VERSION_MINOR "${NVCC_OUT}")
   set(CUDA_VERSION "${CUDA_VERSION_MAJOR}${CUDA_VERSION_MINOR}")
@@ -556,12 +556,12 @@
     if(WITH_COMPILER_CCACHE AND CCACHE_PROGRAM)
       add_custom_command(
         OUTPUT ${cuda_file}
-        COMMAND ${CCACHE_PROGRAM} ${cuda_nvcc_executable} ${_cuda_nvcc_args}
+        COMMAND ${CCACHE_PROGRAM} nvcc ${_cuda_nvcc_args}
         DEPENDS ${kernel_sources})
     else()
       add_custom_command(
         OUTPUT ${cuda_file}
-        COMMAND ${cuda_nvcc_executable} ${_cuda_nvcc_args}
+        COMMAND nvcc ${_cuda_nvcc_args}
         DEPENDS ${kernel_sources})
     endif()
 
@@ -925,7 +925,7 @@
         ${SRC_KERNEL_DEVICE_OPTIX_HEADERS}
         ${SRC_UTIL_HEADERS}
       COMMAND
-        ${cuda_nvcc_executable}
+        nvcc
         --ptx
         -arch=${arch}
         ${CUDA_NVCC_FLAGS}
