diff '--color=auto' -ur pytorch-v2.13.0.orig/third_party/kineto/libkineto/CMakeLists.txt pytorch-v2.13.0/third_party/kineto/libkineto/CMakeLists.txt
--- pytorch-v2.13.0.orig/third_party/kineto/libkineto/CMakeLists.txt	2026-07-08 20:57:07.050256279 +0200
+++ pytorch-v2.13.0/third_party/kineto/libkineto/CMakeLists.txt	2026-07-08 20:59:07.235954887 +0200
@@ -222,7 +222,7 @@
 set(IPCFABRIC_INCLUDE_DIR "${DYNOLOG_INCLUDE_DIR}/dynolog/src/ipcfabric/")
 add_subdirectory("${IPCFABRIC_INCLUDE_DIR}")
 
-if(NOT TARGET fmt::fmt-header-only)
+if(FALSE)
   if(NOT FMT_SOURCE_DIR)
     set(FMT_SOURCE_DIR "${LIBKINETO_THIRDPARTY_DIR}/fmt"
       CACHE STRING "fmt source directory from submodules")
@@ -248,12 +248,12 @@
 target_include_directories(kineto_base SYSTEM PUBLIC
       $<BUILD_INTERFACE:${DYNOLOG_INCLUDE_DIR}>
       $<BUILD_INTERFACE:${IPCFABRIC_INCLUDE_DIR}>)
-target_link_libraries(kineto_base PRIVATE $<BUILD_INTERFACE:fmt::fmt-header-only>)
+target_link_libraries(kineto_base PRIVATE fmt)
 
 target_include_directories(kineto_api PUBLIC
       $<BUILD_INTERFACE:${LIBKINETO_DIR}>
       $<BUILD_INTERFACE:${LIBKINETO_INCLUDE_DIR}>)
-target_link_libraries(kineto_api PRIVATE $<BUILD_INTERFACE:fmt::fmt-header-only>)
+target_link_libraries(kineto_api PRIVATE fmt)
 
 if(KINETO_LIBRARY_TYPE STREQUAL "static")
   set(BUILD_SHARED_LIBS OFF)
diff '--color=auto' -ur pytorch-v2.13.0.orig/third_party/kineto/libkineto/src/ApproximateClock.h pytorch-v2.13.0/third_party/kineto/libkineto/src/ApproximateClock.h
--- pytorch-v2.13.0.orig/third_party/kineto/libkineto/src/ApproximateClock.h	2026-07-08 19:44:32.000000000 +0200
+++ pytorch-v2.13.0/third_party/kineto/libkineto/src/ApproximateClock.h	2026-07-08 20:59:48.783850714 +0200
@@ -10,6 +10,7 @@
 
 #pragma once
 
+#include <algorithm>
 #include <array>
 #include <chrono>
 #include <cstddef>
