diff '--color=auto' -ur pytorch-2.13.0.orig/torch/utils/cpp_extension.py pytorch-2.13.0/torch/utils/cpp_extension.py
--- pytorch-2.13.0.orig/torch/utils/cpp_extension.py	2026-07-08 20:33:39.198194572 +0200
+++ pytorch-2.13.0/torch/utils/cpp_extension.py	2026-07-08 20:36:27.258362414 +0200
@@ -901,7 +901,7 @@
             try:
                 original_compiler = self.compiler.compiler_so
                 if _is_cuda_file(src):
-                    nvcc = [_join_rocm_home('bin', 'hipcc') if IS_HIP_EXTENSION else _join_cuda_home('bin', 'nvcc')]
+                    nvcc = [_join_rocm_home('bin', 'hipcc') if IS_HIP_EXTENSION else 'nvcc']
                     self.compiler.set_executable('compiler_so', _wrap_compiler(nvcc))
                     if isinstance(cflags, dict):
                         cflags = cflags['nvcc']
@@ -1103,7 +1103,7 @@
                         if IS_HIP_EXTENSION:
                             nvcc = _get_hipcc_path()
                         else:
-                            nvcc = _join_cuda_home('bin', 'nvcc')
+                            nvcc = 'nvcc'
                         if isinstance(self.cflags, dict):
                             cflags = self.cflags['nvcc']
                         elif isinstance(self.cflags, list):
@@ -3151,7 +3151,7 @@
             if IS_HIP_EXTENSION:
                 nvcc = _get_hipcc_path()
             else:
-                nvcc = _join_cuda_home('bin', 'nvcc')
+                nvcc = 'nvcc'
             nvcc = _shell_join(_wrap_compiler(nvcc))
         config.append(f'nvcc = {nvcc}')
     if with_sycl or sycl_dlink_post_cflags:
